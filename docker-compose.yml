version: "3"
services:
  mongo-container:
    image: mongo
    volumes:
      - ./data:/data/db
    ports:
      - "27017:27017"
  node-server-template-cont:
    restart: always
    build: .
    ports:
      - "5005:5000"
    links:
      - mongo-container

